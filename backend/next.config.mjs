/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // No need to manually expose env variables here
  // Next.js automatically makes .env* files available
};

export default nextConfig;